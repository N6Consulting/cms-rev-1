{
  "name": "n6-website",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "start": "hugo server -s site",
    "build": "hugo -s site --minify"
  },
  "devDependencies": {
    "postcss": "^8.0.0",
    "postcss-cli": "^11.0.0",
    "postcss-import": "^16.0.0",
    "postcss-preset-env": "^10.0.0",
    "cssnano": "^7.0.0"
  }
}
